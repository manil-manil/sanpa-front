import type { NextPage } from "next";
import TreePage from "../src/components/TreePage";

const Home: NextPage = () => {
  return (
    <div>
      <TreePage />
    </div>
  );
};

export default Home;
